#pragma kernel CopySurfaceMaterials

#include "Assets/Compute/Voxels/Include/TriangleMaterialSet.hlsl"

StructuredBuffer<TriangleMaterialSet> Source;
RWStructuredBuffer<TriangleMaterialSet> Destination;

uint Count;

[numthreads(64, 1, 1)]
void CopySurfaceMaterials(uint id : SV_DispatchThreadID)
{
    if (id >= Count)
    {
        return;
    }

    Destination[id] = Source[id];
}
